#include <stdio.h>
/**
* main - Entry point
* Return: Always 0 (Success)
*/

int main(void)
{
	int i;
	for (i = 0; i < 100; i++)
	{
	if (i / 10 < i % 10)
	if (i < 89)
	{
	
		putchar(44);
		putchar(32);
	}
	}
		putchar('\n');
		return (0);
}
